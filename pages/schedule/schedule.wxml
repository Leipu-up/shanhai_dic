<view class="schedule-page">
    <!-- 紧凑型筛选栏 - 三行布局 -->
    <view class="filter-bar compact">
        <!-- 第一行：日期范围 -->
        <view class="filter-row compact date-row">
            <view class="filter-item date-item">
                <picker mode="date" value="{{startDate}}" bindchange="onStartDateChange">
                    <view class="date-picker compact">
                        <text class="date-icon">📅</text>
                        <text class="date-text">{{startDate ? startDate : '开始日期'}}</text>
                    </view>
                </picker>
            </view>
            <view class="filter-item date-item">
                <picker mode="date" value="{{endDate}}" bindchange="onEndDateChange">
                    <view class="date-picker compact">
                        <text class="date-icon">📅</text>
                        <text class="date-text">{{endDate ? endDate : '结束日期'}}</text>
                    </view>
                </picker>
            </view>
        </view>

        <!-- 第二行：员工和状态筛选 -->
        <view class="filter-row compact select-row">
            <view class="filter-item staff-item">
                <picker mode="selector" range="{{staffOptions}}" range-key="name" value="{{selectedStaffIndex}}"
                    bindchange="onStaffChange">
                    <view class="filter-select compact">
                        <text class="filter-icon">👩‍💼</text>
                        <text class="filter-text">{{selectedStaffName || '全部员工'}}</text>
                        <text class="dropdown-arrow">▼</text>
                    </view>
                </picker>
            </view>

            <view class="filter-item status-item">
                <picker mode="selector" range="{{statusOptions}}" range-key="name" value="{{selectedStatusIndex}}"
                    bindchange="onStatusChange">
                    <view class="filter-select compact">
                        <text class="filter-icon">📊</text>
                        <text class="filter-text">{{selectedStatusName || '全部状态'}}</text>
                        <text class="dropdown-arrow">▼</text>
                    </view>
                </picker>
            </view>
        </view>

        <!-- 第三行：查询和重置按钮 -->
        <view class="filter-row compact button-row">
            <button class="btn-reset original" bindtap="resetFilters">
                <text class="btn-icon">🔄</text>
                <text class="btn-text">重置</text>
            </button>
            <button class="btn-search original" bindtap="searchAppointments">
                <text class="btn-icon">🔍</text>
                <text class="btn-text">查询</text>
            </button>
        </view>
    </view>

    <!-- 排班列表 -->
    <view class="schedule-list">
        <!-- 统计信息 -->
        <view class="stats-info" wx:if="{{scheduleList.length > 0}}">
            <view class="stats-item">
                <text class="stats-number">{{onDutyCount}}</text>
                <text class="stats-label">在岗</text>
            </view>
            <view class="stats-item">
                <text class="stats-number">{{offDutyCount}}</text>
                <text class="stats-label">休息</text>
            </view>
            <view class="stats-item">
                <text class="stats-number">{{totalAppointments}}</text>
                <text class="stats-label">总预约</text>
            </view>
        </view>

        <!-- 排班表格 -->
        <view class="schedule-table" wx:if="{{scheduleList.length > 0}}">
            <!-- 表头 -->
            <view class="table-header">
                <view class="header-cell">员工姓名</view>
                <view class="header-cell">在岗状态</view>
                <view class="header-cell">预约客户数</view>
                <view class="header-cell">排班日期</view>
                <view class="header-cell">操作</view>
            </view>

            <!-- 表格内容 -->
            <view class="table-body">
                <view class="table-row" wx:for="{{scheduleList}}" wx:key="id">
                    <view class="table-cell">
                        <view class="staff-info">
                            <image class="staff-avatar" src="{{item.staffAvatar || '/images/default-avatar.png'}}">
                            </image>
                            <text class="staff-name">{{item.staffName}}</text>
                        </view>
                    </view>

                    <view class="table-cell">
                        <view class="status-badge {{item.status === '在岗' ? 'on-duty' : 'off-duty'}}">
                            {{item.status}}
                        </view>
                    </view>

                    <view class="table-cell">
                        <view class="appointment-count">
                            <text class="count-number">{{item.appointmentCount}}</text>
                            <text class="count-label">人</text>
                        </view>
                    </view>

                    <view class="table-cell">
                        <view class="schedule-date">
                            <text class="date-day">{{item.scheduleDate.split('-')[2]}}</text>
                            <text class="date-month">{{item.scheduleDate.split('-')[1]}}月</text>
                        </view>
                    </view>

                    <view class="table-cell actions">
                        <button class="action-btn edit" size="mini" bindtap="editSchedule" data-id="{{item.id}}">
                            <text class="action-icon">✏️</text>
                        </button>
                        <button class="action-btn delete" size="mini" bindtap="deleteSchedule" data-id="{{item.id}}">
                            <text class="action-icon">🗑️</text>
                        </button>
                    </view>
                </view>
            </view>
        </view>

        <!-- 空状态 -->
        <view wx:else class="empty-state">
            <view class="empty-icon">📅</view>
            <text class="empty-text">暂无排班信息</text>
            <button class="btn-primary" bindtap="showAddModal">新增排班</button>
        </view>

        <!-- 分页加载提示 -->
        <view class="load-more-container" wx:if="{{scheduleList.length > 0}}">
            <view wx:if="{{isLoading}}" class="loading-more">
                <view class="loading-spinner"></view>
                <text class="loading-text">正在加载更多...</text>
            </view>

            <view wx:if="{{!hasMore && totalCount > 0}}" class="no-more-data">
                <text class="no-more-text">已加载全部排班</text>
            </view>

            <view wx:if="{{hasMore && !isLoading}}" class="load-more-tip">
                <text class="load-more-text">上拉加载更多</text>
            </view>
        </view>
    </view>

    <!-- 新增排班按钮 -->
    <view class="add-btn-container" wx:if="{{scheduleList.length > 0}}">
        <button class="add-btn" bindtap="showAddModal">
            <text class="add-icon">+</text>
            <text class="add-text">新增排班</text>
        </button>
    </view>

    <!-- 新增/编辑排班弹窗组件 -->
    <schedule-modal id="scheduleModal" bind:confirm="handleScheduleConfirm" bind:cancel="hideScheduleModal" />
</view>